import React, { ReactElement } from 'react'

interface Props {
    
}

function Permission({}: Props): ReactElement {
    return (
        <div>
            <h1>Permission Denied</h1>
        </div>
    )
}

export default Permission
